services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////data/accounting.db
      - SQLITE_DATA_DIR=/data
    volumes:
      - ./data:/data
